{
  "name": "genetiks",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Iniciando build...' && cd \"Plataforma Inteligente/frontend\" && echo 'Instalando dependências...' && npm install && echo 'Construindo aplicação...' && npm run build",
    "vercel-build": "npm run build"
  }
}